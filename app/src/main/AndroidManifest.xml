<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="marcobarrios.biotasp2" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ActivityNewActividad"
            android:label="Crear Actividad">
        </activity>

        <activity
            android:name=".ActivityDetailActividad"
            android:label="Detalle de la Actividad">
        </activity>

        <activity
            android:name=".DialogSelect"
            android:label="Seleccionar"
            android:theme="@style/Base.Theme.AppCompat.Light.Dialog">
        </activity>

    </application>

</manifest>
